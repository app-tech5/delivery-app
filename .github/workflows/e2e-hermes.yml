# Build debug APK once → Hermes CDP on emulator (reuse APK artifact).
name: E2E Hermes

on:
  push:
    branches: [codecanyon-sources, main, master]
  pull_request:
  workflow_dispatch:
    inputs:
      reuse_run_id:
        description: "Skip APK build — reuse android-debug-apk from this Actions run id"
        required: false
        type: string

concurrency:
  group: e2e-hermes-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  EXPO_PUBLIC_API_URL: https://good-foods.digitaldienste.fr/api
  EXPO_PUBLIC_DEMO_MODE: "true"
  EXPO_PUBLIC_DEMO_EMAIL: driver@demo.com
  EXPO_PUBLIC_DEMO_PASSWORD: driver123

jobs:
  build-debug-apk:
    name: Build Android debug APK (artifact)
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.reuse_run_id == '' }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v3
      - name: Write .env
        run: |
          cat > .env <<EOF
          EXPO_PUBLIC_API_URL=${{ env.EXPO_PUBLIC_API_URL }}
          EXPO_PUBLIC_DEMO_MODE=true
          EXPO_PUBLIC_DEMO_EMAIL=driver@demo.com
          EXPO_PUBLIC_DEMO_PASSWORD=driver123
          EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
          EOF
          sed -i 's/^[[:space:]]*//' .env
      - run: npm ci
      - name: Patch ABI for CI emulator (x86_64)
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path("android/app/build.gradle")
          t = p.read_text()
          t2 = t.replace('include "arm64-v8a"', 'include "x86_64"')
          if t == t2:
              raise SystemExit("ABI patch failed")
          p.write_text(t2)
          print("ABI -> x86_64")
          PY
      - name: Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            android/.gradle
          key: gradle-debug-${{ runner.os }}-${{ hashFiles('android/**/*.gradle*', 'package-lock.json') }}
      - name: Assemble debug APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon
      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: android-debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 3

  hermes-login-home:
    name: Hermes login→home (reuse APK)
    needs: build-debug-apk
    if: ${{ always() && (needs.build-debug-apk.result == 'success' || needs.build-debug-apk.result == 'skipped') }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - name: Write .env for Metro
        run: |
          cat > .env <<EOF
          EXPO_PUBLIC_API_URL=${{ env.EXPO_PUBLIC_API_URL }}
          EXPO_PUBLIC_DEMO_MODE=true
          EXPO_PUBLIC_DEMO_EMAIL=driver@demo.com
          EXPO_PUBLIC_DEMO_PASSWORD=driver123
          EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
          EOF
          sed -i 's/^[[:space:]]*//' .env
      - run: npm ci
      - name: Download APK from this run
        if: ${{ needs.build-debug-apk.result == 'success' }}
        uses: actions/download-artifact@v4
        with:
          name: android-debug-apk
          path: apk-out
      - name: Download APK from prior run (reuse)
        if: ${{ needs.build-debug-apk.result == 'skipped' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          test -n "${{ inputs.reuse_run_id }}"
          mkdir -p apk-out
          gh run download "${{ inputs.reuse_run_id }}" -n android-debug-apk -D apk-out -R "${{ github.repository }}"
          find apk-out -type f
      - name: Locate APK
        id: apk
        run: |
          APK=$(find apk-out -name '*.apk' | head -n 1)
          test -n "$APK"
          echo "apk=$APK" >> "$GITHUB_OUTPUT"
          ls -lh "$APK"
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Emulator + Metro + Hermes smoke
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          arch: x86_64
          target: google_apis
          profile: pixel_6
          force-avd-creation: true
          disable-animations: true
          emulator-options: -no-snapshot -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
          script: |
            bash -euo pipefail <<'SCRIPT'
            adb install -r "${{ steps.apk.outputs.apk }}"
            adb reverse tcp:8081 tcp:8081
            npx expo start --port 8081 --localhost > /tmp/metro.log 2>&1 &
            METRO_PID=$!
            echo "Metro PID $METRO_PID"
            for i in $(seq 1 90); do
              if curl -sf http://127.0.0.1:8081/status >/dev/null; then
                echo "Metro ready"
                break
              fi
              sleep 2
            done
            curl -sf http://127.0.0.1:8081/status >/dev/null
            adb shell am start -n com.goodfood.driver/.MainActivity
            for i in $(seq 1 60); do
              if curl -sf http://127.0.0.1:8081/json/list | grep -q webSocketDebuggerUrl; then
                echo "Hermes CDP target ready"
                break
              fi
              sleep 3
            done
            curl -sf http://127.0.0.1:8081/json/list | head -c 500 || true
            node scripts/hermes/smoke-login-home.js
            kill $METRO_PID || true
            SCRIPT
      - name: Upload Metro log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: metro-log
          path: /tmp/metro.log
          if-no-files-found: ignore
